import React, { Component } from 'react';

import s from './VideoDetails.scss';

export default class VideoDetails extends Component {
  render() {
    return (
      <div>
        <h1>VideoDetails</h1>
      </div>
    );
  }
}
